<h1>Einstellungen</h1>

{{#each model as |liste|}}
eine liste: {{liste.name}}, id: {{liste.id}}<br>
{{/each}}

<button onclick={{action "addentry"}} >nochwas</button>

<button class="mdl-button mdl-js-button mdl-button--fab mdl-button--colored">
  <i class="material-icons">add</i>
</button>

<input type="file" onchange={{action "handleFiles"}} accept=".csv">

{{outlet}}
